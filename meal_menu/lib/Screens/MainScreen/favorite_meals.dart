import 'package:flutter/material.dart';

class FavoriteMeals extends StatelessWidget {
  const FavoriteMeals({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text('Favorite Meals');
  }
}
